
<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <title>Manifest</title>
    </head>
    <body>
        <h2>Please fill in the sender manifest form for your file</h2>
        <form method="POST" action="/upload">
          {{range .Metadata.Fields}}
          <div>
            <label for="{{.FieldName}}">{{FixNames .FieldName}}</label>
            {{if not .AllowedValues}}
              <input name="{{.FieldName}}" type="text" />
            {{ else }}
              <select>
                {{range .AllowedValues}}
                  <option value="{{.}}">{{.}}</option>
                {{end}}
              </select>
            {{ end }}
          </div>
          {{end}}
          <button type="submit">Next</button>
        </form>
    </body>
</html>