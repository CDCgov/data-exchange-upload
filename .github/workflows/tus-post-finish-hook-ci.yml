name: "Tus Post-Finish Hook CI"

on:
  workflow_dispatch:
  pull_request:
    paths:
      - tus/file-hooks/post-finish/**

defaults:
  run:
    working-directory: tus/file-hooks/post-finish

jobs:
  run-tests:
    uses: ./.github/workflows/tus-hooks-ci-template.yml
    with:
      WORKING_DIRECTORY: tus/file-hooks/post-finish
