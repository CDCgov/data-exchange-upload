name: "DEV - Tus Post-Finish Hook"

on:
  workflow_dispatch:
  push:
    branches:
      - main
    paths:
      - tus/file-hooks/post-finish/**

jobs:
  dev:
    uses: ./.github/workflows/remote-cd-trigger-template.yml
    with:
      WORKFLOW: tus-post-finish-hook-cd-dev.yml
    secrets: inherit
