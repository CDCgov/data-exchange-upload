import { test } from '@playwright/test';

import { start } from '../index'; // Assuming start and getTraceResponse are exported

test('upload and get trace response', async ({}) => {
  
  await start(); // Make sure this function is adapted to fit within a testing context if necessary

  
});
