{
  "name": "playwright",
  "version": "1.0.0",
  "description": "",
  "main": "dist/main.js",
  "scripts": {
    "build": "npx tsc",
    "prep-reports": "mkdir -p test-reports && mkdir -p test-results",
    "wait": "./wait-for-it.sh ${UI_URL:-http://localhost:8081}",
    "test": "npm run wait; npx playwright test",
    "test:ci": "npm run wait; npx playwright test > ./test-reports/${TEST_REPORT_GITHUB:-github-report.txt}",
    "test:conditional": "if test \"$CI\" = true; then npm run test:ci; else npm run test; fi",
    "test:docker": "npm run prep-reports; npm run test:conditional"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "tus-js-client": "^4.0.1",
    "uuid": "^9.0.1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@axe-core/playwright": "^4.10.0",
    "@playwright/test": "1.48.0",
    "@types/express": "^4.17.1",
    "@types/node": "^20.11.22",
    "ts-node": "^10.9.2",
    "typescript": "^5.3.3"
  }
}
